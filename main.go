package main

func main() {
	// Load/init persistence, doesn't make sense to proceed without it.
	// Identify what command is being used.
	// Call logic that makes sense for that command.
	// Persist changes and return
}
